# blacklist.json contains the words that are considered spam and timeout/ban worthy. There is a category
# ("timeout") for words that start with warnings, but once a user has been warned enough, result in a ban.
# The other category ("ban") is for words that result in an instant ban.

def parse_message(msg, username):
    """Parse a chat message for possible timeouts or bans."""
    pass
